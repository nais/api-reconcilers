dir: "{{ .InterfaceDir }}"
filename: "mock_{{ .InterfaceName | snakecase }}.go"
template: testify
packages:
  github.com/nais/api-reconcilers/internal/azureclient:
    interfaces:
      Client:
  github.com/nais/api-reconcilers/internal/reconcilers:
    interfaces:
      Reconciler:
  github.com/nais/api-reconcilers/internal/reconcilers/dependencytrack:
    interfaces:
      Client:
  github.com/nais/api-reconcilers/internal/reconcilers/github/team:
    interfaces:
      TeamsService:
      GraphClient:
  github.com/nais/api-reconcilers/internal/reconcilers/grafana:
    interfaces:
      UsersClientService:
      TeamsClientService:
      AccessControlClientService:
      ServiceAccountsClientService:
      AdminUsersClientService:
      ProvisioningClientService:
